#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;
    while (T--) {
        int X, Y;
        cin >> X >> Y;

        int B = (Y - X) / 2;
        int A = B + X;

        cout << A << " " << B << endl;
    }
    return 0;
}
