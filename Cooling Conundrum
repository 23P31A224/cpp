#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int X, Y;
        cin >> X >> Y;

        int ans = 0;

        for (int k = X; k > Y; k--) {
            ans += (k + 9) / 10;
        }

        cout << ans << endl;
    }

    return 0;
}
